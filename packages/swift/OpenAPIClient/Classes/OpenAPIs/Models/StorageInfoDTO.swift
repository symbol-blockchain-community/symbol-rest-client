//
// StorageInfoDTO.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

public struct StorageInfoDTO: Codable, JSONEncodable, Hashable {

    /** Number of blocks stored. */
    public var numBlocks: Int
    /** Number of transactions stored. */
    public var numTransactions: Int
    /** Number of accounts created. */
    public var numAccounts: Int

    public init(numBlocks: Int, numTransactions: Int, numAccounts: Int) {
        self.numBlocks = numBlocks
        self.numTransactions = numTransactions
        self.numAccounts = numAccounts
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case numBlocks
        case numTransactions
        case numAccounts
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(numBlocks, forKey: .numBlocks)
        try container.encode(numTransactions, forKey: .numTransactions)
        try container.encode(numAccounts, forKey: .numAccounts)
    }
}

